import { Browser, BrowserConfig } from './BrowserConfig';

export const config: BrowserConfig = {
    browser: Browser.CHROME,
    headless: false
};

export const BASE_URL = 'https://tk-gr.demo.goodrabbit.tech/signin';